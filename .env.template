# ─── SAP Connection (abaper-ts, abaper-mcp, abaper-go) ───
SAP_HOST=
SAP_CLIENT=100
SAP_USERNAME=
SAP_PASSWORD=

# ─── GitHub (abaper-go OAuth + private module access) ───
GITHUB_CLIENT_ID=Ov23li2XoloIy5OdKjKJ
GITHUB_CLIENT_SECRET=
GH_PAT=
GITHUB_TOKEN=

# ─── LLM API Keys (cai-llm-router) ───
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GROQ_API_KEY=
SARVAM_API_KEY=

# ─── abaper-mcp ───
ABAPER_MODE=sse
ABAPER_USE_LEGACY_SSE=false
ABAPER_HTTP_HOST=0.0.0.0
ABAPER_HTTP_PORT=8015
ABAPER_TS_URL=http://abaper-ts:8080

# ─── abaper-bff ───
ABAPER_BFF_SERVER_PORT=8080
ABAPER_BFF_BACKENDS_ABAPER_TS=http://abaper-ts:8080
ABAPER_BFF_BACKENDS_ABAPER=http://abaper-go:8086
ABAPER_BFF_BACKENDS_ABAPER_MCP=http://abaper-mcp:8015
ABAPER_BFF_BACKENDS_LLM_ROUTER=http://cai-bff:8080
ABAPER_BFF_LOG_LEVEL=debug

# ─── cai-bff ───
HTTP_PORT=8080
GRPC_PORT=9090
METRICS_PORT=9091
NATS_ENABLED=true
DEV=true
ALLOW_PATH_BASED_REALM=false

# ─── cai-llm-router ───
NATS_URL=nats://nats:4222
CONFIG_DIR=/config
LOG_LEVEL=debug
LOG_FORMAT=console

# ─── abaper-gw (KrakenD — also read from krakend-compose.json) ───
DOMAIN=localhost
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174
ENV=compose

# ─── Frontend (Docker profile only) ───
VITE_GITHUB_CLIENT_ID=Ov23li2XoloIy5OdKjKJ
LOCAL=1
