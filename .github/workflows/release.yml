name: Release

on:
  push:
    branches: [main]

jobs:
  release:
    uses: bluefunda/release-foundry/.github/workflows/release-please.yml@main
    permissions:
      contents: write
      pull-requests: write
    with:
      release-type: node
    secrets:
      GH_PAT: ${{ secrets.GH_PAT }}
